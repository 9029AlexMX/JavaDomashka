databaseChangeLog:
  -  changeSet:
       id: user-role-create-table
       author:  alexey.fedorov
       changes:
         - createTable:
             columns:
               - column:
                  constraints:
                    primaryKey: true
                    nullable: false
                  name: id
                  type: bigint
                  autoIncrement: true
               - column:
                   name:  name
                   type:  varchar(255)
             tableName:  user_role
       rollback:
         - dropTable:
             tableName: user_role